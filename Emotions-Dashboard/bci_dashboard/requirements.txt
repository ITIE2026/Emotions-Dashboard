PySide6>=6.5
# PySide6_Addons (QtWebEngineWidgets) must be installed separately to a short path
# due to Windows MAX_PATH limits.  Run:
#   python3.11 -m pip install PySide6_Addons==<your_pyside6_version> --target C:\p6 --no-deps --no-user
# Instagram reels playback uses pywebview with the Windows Edge WebView2 runtime.
# The Edge WebView2 runtime is an OS component and is not installed via pip.
pywebview>=6.1
pyqtgraph>=0.13
numpy>=1.24
pandas>=2.0
pyserial>=3.5
h5py>=3.10
mne>=1.8
websockets>=12.0
